module.exports = (genres) =>{
    
    let total = Object.values(genres)

    total = total.reduce(function(a, b){return a+b;})

    let precentGenres = Object.entries(genres).forEach(genre => {
       
      
    });
      
}